using OpenQA.Selenium;

namespace SeleniumCSharpNUnitFramework.Framework.Drivers;

public class DriverManager
{
    /*private DriverManager()
    {
    }

    private static readonly ThreadLocal<IWebDriver> driver = new ThreadLocal<IWebDriver>();

    internal static IWebDriver GetDriver()
    {
        return driver.Value;
    }

    internal static void setDriver(IWebDriver driverref)
    {
           
        driver.Value= driverref;
           
    }

    internal static void Unload()
    {
        driver.Dispose();
    }*/
}